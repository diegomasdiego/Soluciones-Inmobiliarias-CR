# Build mode for the published Claude artifact (npm run artifact): no database connection,
# the artifact viewer blocks network requests, so the site uses the bundled sample listings.
VITE_CONVEX_URL=
